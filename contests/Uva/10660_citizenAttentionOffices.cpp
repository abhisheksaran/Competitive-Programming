#include<bits/stdc++.h>
using namespace std;
using pii=pair<int ,int>;

bool sortBySec(const pii &a,const pii &b ){
  return (a.second<b.second);
}

int main(){
  int t;
  /*cin>>t;
  for( int i=0;i<t;i++){
    int n;
    cin>>n;
    vector<pair<pii,int > > cities;
    for(int j=0;j<n;j++){
      int u,v,w;
      cin>>u>>v>>w;
      cities.push_back({{u,v},w});
    }*/
//5 nested loop length 25
//get minm distance of each city from offices
//and thus add all such city minm distances
//and finally find out which 5 offices miniizes this sum.

  cout<<"Easy one!,  hint has been written in code\n";
  //}
}
